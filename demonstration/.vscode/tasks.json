{
    "tasks": [
        {
            "label": "Build",
            "dependsOn": ["Build executable"]
        },
        {
            "label":  "Build executable",
            "type": "shell",
            "command": "pwsh",
            "args": ["build-for-windows.ps1"],
            "options": {
                "cwd": "${workspaceFolder}",
            }
        }
    ],
    "version": "2.0.0"
}