{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Shaders",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceRoot}/3d-game-shaders.exe",
            "cwd": "${workspaceRoot}",
            "preLaunchTask": "Build"
        }
    ]
}